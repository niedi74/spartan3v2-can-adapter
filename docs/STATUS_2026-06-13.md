# STATUS 2026-06-13 — Spartan Hub (Motorraum)

> **Einstieg für neuen Agent:** Branch `main` ist die einzige aktive Entwicklungslinie.
> Build: `pio run -e motorraum` · Flash: `COM16`

## Kurzstand

| Bereich | Status |
|---------|--------|
| **Hub Firmware (`main`)** | ESP-NOW Broadcast an Displays, 123TUNE BLE gehärtet, Event-Log, Web-GUI Diagnose |
| **Flash Hub** | Offen — `pio run -e motorraum -t upload --upload-port COM16` |
| **M5 (`m5stack-123`)** | `feature/spartan-live-display` @ `c3189c2` — **ESP-NOW Empfang** (Bus/Kanal 6), HTTP-Fallback, IP `.2` |
| **Waveshare** | `-ESP32-S3-2-8-Zoll-IPS-Touchscreen-VDO-Copit-T2B` · `cursor/webgui-ota-c56e` @ `554b30a` — **ESP-NOW Empfang**, HTTP-Fallback, IP `.3` |
| **Display-Datenweg** | Hub sendet ESP-NOW (250 ms) — M5 + Waveshare empfangen im **Bus-Profil** (`Spartan3-Setup`, ch6); HTTP `/api/status` für NTP/BM6/Speed |

## ESP-NOW Kette (13.06. ~19:00 UTC)

| Gerät | Commit | ESP-NOW | Flash |
|-------|--------|---------|-------|
| Hub | `7b47713` | **Send** | Offen (COM16) |
| M5 Dial | `c3189c2` | **Recv** + HTTP fallback | Offen (COM4) |
| Waveshare 2.8″ | `554b30a` | **Recv** + HTTP fallback | Offen (COM13) |

Gemeinsamer Frame: `include/spartan_cockpit_frame.h` (14 Byte + CRC-8).

## Hub — was `main` kann

- **Lambda** per CAN (Spartan 3 v2), UART-Konfig, optional Demo/Analog
- **123TUNE+** per BLE Central (NUS), Reconnect-State-Machine, Radio-Priorität
- **BM6** Batterie-Monitor (dual-slot main/aux)
- **Cockpit → Displays** per **ESP-NOW Broadcast** (250 ms, 14-Byte-Frame + CRC-8)
- **BLE Display Server aus** (`ENABLE_BLE_DISPLAY=0`) — kein instabiles Multi-GATT mehr
- **Event-Log:** `GET /log/events`, `/log/events.csv`
- **CSV Fahrt-Log** auf SPIFFS
- **NTP / time_epoch** — Hub als Zeitmaster
- **Reed-Speed** GPIO 27, Heater-ADC GPIO 35
- **Live-Debug:** Handy-Hotspot `Android-AP1` → `docs/road-live-debug.md`

## Build-Flags (`motorraum`)

```
ENABLE_BLE_HUB=1          # 123TUNE BLE Central
ENABLE_BLE_DISPLAY=0      # Displays NICHT per BLE
ENABLE_ESP_NOW_HUB=1      # Cockpit Broadcast
ESP_NOW_WIFI_CHANNEL=6
```

Display-Clients (`m5stack-123`, Waveshare):

```
ENABLE_ESP_NOW_CLIENT=1
ESP_NOW_WIFI_CHANNEL=6
```

## Bus-WLAN Matrix (im Auto)

| Gerät | SSID | IP | Port |
|-------|------|-----|------|
| Hub AP | `Spartan3-Setup` | `192.168.4.1` | — |
| M5 Dial | (Client) | `192.168.4.2` | COM4 |
| Waveshare | (Client) | `192.168.4.3` | COM13 |
| Hub (Motorraum) | — | — | COM16 |

## Hardware-Pins (motorraum)

| Signal | GPIO |
|--------|------|
| CAN RX/TX | 25 / 26 |
| Spartan UART RX/TX | 16 / 17 |
| Speed Reed | 27 |
| Heater ADC | 35 |
| I2C LCD | 21 / 22 |

## Offene nächste Schritte

1. **Drei Geräte flashen:** Hub (COM16), M5 (COM4), Waveshare (COM13)
2. Bus-Profil aktivieren (`Spartan3-Setup`) — ESP-NOW läuft nur im Bus-Modus
3. Fahrzeugtest: Anzeige `HUB ESP-NOW` / `esp_now_fresh=true` auf Displays
4. Optional: `work`-Branch löschen, PR #5–#7 schließen (Inhalt in `main`)

## Repos

| Repo | Branch | Rolle |
|------|--------|-------|
| `niedi74/spartan3v2-can-adapter` | **`main`** | Motorraum-Hub ESP32 |
| `niedi74/m5stack-123` | `feature/spartan-live-display` | M5 Dial Cockpit |
| `niedi74/-ESP32-S3-2-8-Zoll-IPS-Touchscreen-VDO-Copit-T2B` | `cursor/webgui-ota-c56e` | 2.8″ Round Display |
| `niedi74/norbi-espnow` | — | Monorepo (leer, Push-Skript in PR #8) |
| `niedi74/waveshare-vdo-clock` | — | Alias-Repo (leer) |

## Doku-Index

| Datei | Inhalt |
|-------|--------|
| `docs/road-live-debug.md` | Laptop ↔ Hub über Handy-Hotspot |
| `docs/espnow-gateway-architecture.md` | ESP-NOW Frame, Client-Checkliste |
| `docs/handoff-fahrt-esphub-2026-06.md` | Ausführliches Handoff für große Session |
| `docs/handoff_2026-05-26.md` | Älterer Stand, 123-BLE Diagnose-Regeln |
| `include/spartan_cockpit_frame.h` | Shared Binary Frame (Hub → Displays) |

## Git — flache Struktur

- **`main`** = produktiver Stand (ehemals `work`)
- Alte Feature-Branches (`cursor/*`, `codex/esp32s3-*`) wurden aufgeräumt
- PR #7 (ESP-NOW) ist in `main` integriert — PR formal noch offen
